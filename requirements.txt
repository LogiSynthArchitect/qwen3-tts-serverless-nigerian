# Qwen3-TTS Serverless Requirements

# Core ML framework
# (Installed separately in bootstrap.sh with CUDA 12.8 support)
# torch==2.9.1
# torchaudio==2.9.1

# RunPod serverless
runpod>=1.6.0

# Audio processing
librosa
soundfile
numpy>=1.26.0

# HTTP/server dependencies
boto3>=1.26.0

# Model download acceleration
hf_transfer

# Optional: FlashAttention 2 for GPU memory optimization
# (Installed separately in bootstrap.sh)
# flash-attn
